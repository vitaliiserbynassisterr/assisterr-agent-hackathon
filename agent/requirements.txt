# Agent Proof-of-Intelligence Demo
# Python 3.11+ required

# Core web framework + server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Solana SDK
solana>=0.34.0
solders>=0.21.0
anchorpy>=0.20.1
base58>=2.1.0

# HTTP client for A2A communication
httpx>=0.25.0

# LLM providers (for judge scoring)
anthropic>=0.40.0

# CLI
click>=8.1.0

# Environment
python-dotenv>=1.0.0

# AgentiPy - install without deps to avoid pip backtracking hell
# agentipy's 100+ transitive deps cause pip to backtrack for 10+ minutes
# We install the deps we actually use above, then agentipy --no-deps
